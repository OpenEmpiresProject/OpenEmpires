#ifndef WIDTHHEIGHT_H
#define WIDTHHEIGHT_H

namespace aion
{
struct WidthHeight
{
    int width;
    int height;

    WidthHeight(int w, int h) : width(w), height(h)
    {
    }
};
} // namespace aion

#endif
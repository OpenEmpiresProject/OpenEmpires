#include "Game.h"
#include "internal/DRSTester.h"
#include "Studio.h"

int main()
{
    game::Game game;
    return game.run();

    // drs::DRSTester tester;
    // tester.load();
    // return 0;

    // game::Studio studio;
    // studio.init();
    // studio.run();
    // return 0;
}
